# 📝 项目文件配置说明

## ✅ 已解决：文档文件被错误编译

### 问题描述
编译时出现错误：
```
error CS1024: 应输入预处理器指令
D:\gitcode\wx4helper\BaiShengVx3Plus\0-资料\00-开发设计目标.md(2,2,2,3): error CS1002: 应输入 ;
```

**原因**: `.csproj` 文件中错误地将 `.md` 文档文件添加为 `Compile` 项。

### 解决方案

在 `BaiShengVx3Plus.csproj` 中添加了排除规则：

```xml
<!-- 排除文档文件夹，防止被编译 -->
<ItemGroup>
  <Compile Remove="0-资料\**" />
  <EmbeddedResource Remove="0-资料\**" />
  <None Remove="0-资料\**" />
</ItemGroup>
```

这样可以：
1. ✅ 排除 `0-资料` 文件夹中的所有文件，防止被编译
2. ✅ 防止以后误添加文档文件到编译中
3. ✅ 文档文件仍然在项目中可见，可以编辑

---

## 🔧 .csproj 文件完整配置

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!-- 排除文档文件夹，防止被编译 -->
  <ItemGroup>
    <Compile Remove="0-资料\**" />
    <EmbeddedResource Remove="0-资料\**" />
    <None Remove="0-资料\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="SunnyUI" Version="3.6.9" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="8.0.0" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.0" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.2" />
    <PackageReference Include="sqlite-net-pcl" Version="1.9.172" />
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="2.1.10" />
    <PackageReference Include="System.Data.SQLite.Core" Version="1.0.118" />
  </ItemGroup>

  <ItemGroup>
    <!-- 自动复制 SQLite 原生 DLL 到输出目录 -->
    <None Include="libs\e_sqlite3.dll" Condition="Exists('libs\e_sqlite3.dll')">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Include="libs\sqlite3.dll" Condition="Exists('libs\sqlite3.dll')">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
```

---

## 📦 NuGet 包说明

| 包名 | 版本 | 用途 |
|------|------|------|
| SunnyUI | 3.6.9 | 现代化 UI 控件库 |
| Microsoft.Extensions.DependencyInjection | 8.0.0 | 依赖注入 |
| Microsoft.Extensions.Hosting | 8.0.0 | 主机构建器 |
| CommunityToolkit.Mvvm | 8.2.2 | MVVM 工具包 |
| sqlite-net-pcl | 1.9.172 | SQLite ORM |
| SQLitePCLRaw.bundle_e_sqlite3 | 2.1.10 | SQLite 原生库 |
| System.Data.SQLite.Core | 1.0.118 | SQLite 核心库 |

---

## 🚀 编译说明

### 方式1: 使用 Visual Studio
```
1. 打开解决方案
2. 按 Ctrl+Shift+B 编译
3. 按 F5 运行
```

### 方式2: 使用命令行
```bash
# 在项目根目录
dotnet build

# 运行
dotnet run
```

### 方式3: 使用批处理文件
```bash
# 双击运行 build.bat
build.bat
```

---

## 📁 项目结构

```
BaiShengVx3Plus/
├── 0-资料/                    # 📝 文档文件夹 (已排除编译)
│   ├── 00-开发设计目标.md
│   ├── 开奖系统实现完成-100percent.md
│   ├── README-开奖系统使用指南.md
│   └── ...
├── Contracts/                 # 接口定义
│   ├── Games/
│   └── Messages/
├── Core/                      # 核心类 (BindingList)
├── Extensions/                # 扩展方法
├── Helpers/                   # 辅助类 (BinggoHelper)
├── Models/                    # 数据模型
│   ├── Api/
│   └── Games/
├── Services/                  # 服务实现
│   ├── Api/
│   ├── Auth/
│   ├── Contact/
│   ├── Games/
│   ├── GroupBinding/
│   ├── Logging/
│   ├── Messages/
│   ├── UserInfo/
│   └── WeChat/
├── ViewModels/                # 视图模型
├── Views/                     # 视图 (WinForms)
├── libs/                      # 第三方库 (SQLite DLL)
├── Data/                      # 数据库文件 (运行时生成)
├── BaiShengVx3Plus.csproj    # 项目文件
├── Program.cs                 # 入口点
└── build.bat                  # 编译脚本
```

---

## ⚠️ 注意事项

### 1. 不要手动添加 .md 文件到编译
如果在 Visual Studio 中不小心将 `.md` 文件的"生成操作"设置为"编译"，会导致编译错误。

**解决**: 在 `.csproj` 中已添加排除规则，自动防止此类错误。

### 2. SQLite DLL 部署
确保 `libs/e_sqlite3.dll` 和 `libs/sqlite3.dll` 存在，编译时会自动复制到输出目录。

### 3. 文档文件夹
`0-资料/` 文件夹中的所有文件都不会被编译，可以安全地添加任何文档。

---

## ✅ 验证配置

### 检查编译是否正常
```bash
dotnet build
```

### 检查输出目录
```bash
ls bin/Debug/net8.0-windows/
```

应包含:
- BaiShengVx3Plus.exe
- e_sqlite3.dll
- sqlite3.dll
- 其他依赖 DLL

---

**配置完成！现在可以正常编译项目了。** 🎉

