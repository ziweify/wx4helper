# 简化版自动投注设计 - 简洁、易维护

## 🎯 设计原则

- ✅ **简单优先**：能用简单方案就不用复杂的
- ✅ **易于理解**：代码清晰，逻辑直观
- ✅ **易于维护**：少量核心类，职责明确
- ✅ **够用就好**：先满足基本需求，再扩展

---

## 🏗️ 简化架构

### 核心思路
1. **不需要独立进程**：浏览器作为控件内嵌在主程序中
2. **不需要复杂通信**：直接调用，不需要 IPC/WebSocket
3. **不需要多实例**：当前只用 1 个，预留扩展接口即可

### 架构图

```
┌────────────────────────────────────────────────┐
│          BaiShengVx3Plus (主程序)              │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │    AutoBetService (自动投注服务)         │ │
│  │    • StartBrowser(platform)              │ │
│  │    • PlaceBet(order)                     │ │
│  │    • GetBalance()                        │ │
│  └──────────┬───────────────────────────────┘ │
│             │                                  │
│  ┌──────────▼───────────────────────────────┐ │
│  │    BrowserManager (浏览器管理器)         │ │
│  │    • CefBrowser (内嵌浏览器控件)         │ │
│  │    • PlatformScript (当前平台脚本)       │ │
│  └──────────────────────────────────────────┘ │
│                                                │
│  平台脚本：YunDing28Script, BingGo28Script... │
└────────────────────────────────────────────────┘
```

---

## 📦 项目结构（极简）

```
BaiShengVx3Plus/
├── Services/
│   └── AutoBet/
│       ├── AutoBetService.cs           # 🔥 核心服务（唯一入口）
│       └── BrowserManager.cs           # 🔥 浏览器管理（封装CEF）
│
├── Scripts/                            # 🔥 平台脚本
│   ├── IPlatformScript.cs              # 脚本接口
│   ├── YunDing28Script.cs              # 云顶28
│   ├── BingGo28Script.cs               # 炳狗28
│   └── ...
│
└── Resources/
    └── js/                             # JavaScript 文件
        ├── yunding28.js
        ├── binggo28.js
        └── ...
```

**只需 3 个核心类 + 平台脚本！**

---

## 💻 核心代码实现

### 1️⃣ AutoBetService（唯一入口，超简单）

```csharp
namespace BaiShengVx3Plus.Services.AutoBet
{
    /// <summary>
    /// 自动投注服务 - 唯一对外接口
    /// </summary>
    public class AutoBetService : IAutoBetService
    {
        private readonly BrowserManager _browserManager;
        private readonly ILogService _logService;
        
        public AutoBetService(ILogService logService)
        {
            _logService = logService;
            _browserManager = new BrowserManager(logService);
        }
        
        /// <summary>
        /// 启动浏览器
        /// </summary>
        public async Task<bool> StartAsync(PlatformType platform)
        {
            try
            {
                await _browserManager.InitializeAsync(platform);
                _logService.Info("AutoBet", $"✅ 浏览器已启动: {platform}");
                return true;
            }
            catch (Exception ex)
            {
                _logService.Error("AutoBet", "启动浏览器失败", ex);
                return false;
            }
        }
        
        /// <summary>
        /// 停止浏览器
        /// </summary>
        public void Stop()
        {
            _browserManager.Dispose();
            _logService.Info("AutoBet", "浏览器已停止");
        }
        
        /// <summary>
        /// 登录平台
        /// </summary>
        public async Task<bool> LoginAsync(string username, string password)
        {
            return await _browserManager.ExecuteLoginAsync(username, password);
        }
        
        /// <summary>
        /// 获取余额
        /// </summary>
        public async Task<decimal> GetBalanceAsync()
        {
            return await _browserManager.ExecuteGetBalanceAsync();
        }
        
        /// <summary>
        /// 投注
        /// </summary>
        public async Task<BetResult> PlaceBetAsync(BetOrder order)
        {
            return await _browserManager.ExecutePlaceBetAsync(order);
        }
        
        /// <summary>
        /// 显示/隐藏浏览器窗口
        /// </summary>
        public void ShowBrowser(bool show)
        {
            _browserManager.SetVisible(show);
        }
    }
}
```

**就这么简单！所有功能都通过这一个类调用。**

---

### 2️⃣ BrowserManager（浏览器管理，封装 CEF）

```csharp
using CefSharp;
using CefSharp.WinForms;

namespace BaiShengVx3Plus.Services.AutoBet
{
    /// <summary>
    /// 浏览器管理器 - 封装 CEF 和脚本执行
    /// </summary>
    public class BrowserManager : IDisposable
    {
        private ChromiumWebBrowser? _browser;
        private IPlatformScript? _currentScript;
        private readonly ILogService _logService;
        private Form? _browserForm;  // 承载浏览器的窗体
        
        public BrowserManager(ILogService logService)
        {
            _logService = logService;
        }
        
        /// <summary>
        /// 初始化浏览器
        /// </summary>
        public async Task InitializeAsync(PlatformType platform)
        {
            // 1. 初始化 CEF（只需一次）
            if (!Cef.IsInitialized)
            {
                var settings = new CefSettings
                {
                    CachePath = Path.Combine(
                        Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
                        "BaiShengVx3Plus", "BrowserCache"),
                    LogSeverity = LogSeverity.Warning
                };
                Cef.Initialize(settings);
            }
            
            // 2. 创建浏览器控件
            _currentScript = CreateScript(platform);
            _browser = new ChromiumWebBrowser(_currentScript.PlatformUrl)
            {
                Dock = DockStyle.Fill
            };
            
            // 3. 创建承载窗体
            _browserForm = new Form
            {
                Text = $"自动投注 - {platform}",
                Width = 1280,
                Height = 800,
                StartPosition = FormStartPosition.CenterScreen
            };
            _browserForm.Controls.Add(_browser);
            
            // 4. 等待页面加载
            await WaitForPageLoadAsync();
            
            // 5. 注入脚本
            await InjectScriptsAsync();
            
            _logService.Info("BrowserManager", "浏览器初始化完成");
        }
        
        /// <summary>
        /// 等待页面加载完成
        /// </summary>
        private async Task WaitForPageLoadAsync()
        {
            var tcs = new TaskCompletionSource<bool>();
            
            _browser.LoadingStateChanged += (sender, args) =>
            {
                if (!args.IsLoading)
                {
                    tcs.TrySetResult(true);
                }
            };
            
            // 如果已经加载完成
            if (!_browser.IsLoading)
            {
                return;
            }
            
            await tcs.Task;
            await Task.Delay(500); // 等待 DOM 稳定
        }
        
        /// <summary>
        /// 注入脚本
        /// </summary>
        private async Task InjectScriptsAsync()
        {
            // 注入通用库
            await ExecuteScriptAsync(@"
                window.bsAutoBet = {
                    log: function(msg) { console.log('[BS] ' + msg); }
                };
            ");
            
            // 注入平台脚本
            var initScript = _currentScript!.GetInitScript();
            await ExecuteScriptAsync(initScript);
        }
        
        /// <summary>
        /// 执行 JavaScript
        /// </summary>
        private async Task<JavascriptResponse> ExecuteScriptAsync(string script)
        {
            return await _browser.EvaluateScriptAsync(script);
        }
        
        /// <summary>
        /// 执行登录
        /// </summary>
        public async Task<bool> ExecuteLoginAsync(string username, string password)
        {
            try
            {
                var script = _currentScript!.GetLoginScript(username, password);
                var result = await ExecuteScriptAsync(script);
                
                // 等待登录完成
                await Task.Delay(2000);
                
                // 检查是否登录成功（可以通过特定元素判断）
                var checkScript = "document.querySelector('.user-info') !== null";
                var checkResult = await ExecuteScriptAsync(checkScript);
                
                return (bool)(checkResult.Result ?? false);
            }
            catch (Exception ex)
            {
                _logService.Error("BrowserManager", "登录失败", ex);
                return false;
            }
        }
        
        /// <summary>
        /// 获取余额
        /// </summary>
        public async Task<decimal> ExecuteGetBalanceAsync()
        {
            try
            {
                var script = _currentScript!.GetBalanceScript();
                var result = await ExecuteScriptAsync(script);
                
                if (result.Success && result.Result != null)
                {
                    return Convert.ToDecimal(result.Result);
                }
                
                return 0;
            }
            catch (Exception ex)
            {
                _logService.Error("BrowserManager", "获取余额失败", ex);
                return 0;
            }
        }
        
        /// <summary>
        /// 执行投注
        /// </summary>
        public async Task<BetResult> ExecutePlaceBetAsync(BetOrder order)
        {
            try
            {
                var script = _currentScript!.GetPlaceBetScript(order);
                var result = await ExecuteScriptAsync(script);
                
                // 等待投注完成
                await Task.Delay(1000);
                
                // 获取投注结果（从页面中提取）
                var checkScript = @"
                    (function() {
                        var success = document.querySelector('.bet-success') !== null;
                        var orderId = document.querySelector('.order-id')?.textContent || '';
                        return { success: success, orderId: orderId };
                    })()
                ";
                var checkResult = await ExecuteScriptAsync(checkScript);
                
                if (checkResult.Success && checkResult.Result != null)
                {
                    dynamic data = checkResult.Result;
                    return new BetResult
                    {
                        Success = data.success,
                        OrderId = data.orderId
                    };
                }
                
                return new BetResult { Success = false, ErrorMessage = "未知错误" };
            }
            catch (Exception ex)
            {
                _logService.Error("BrowserManager", "投注失败", ex);
                return new BetResult { Success = false, ErrorMessage = ex.Message };
            }
        }
        
        /// <summary>
        /// 显示/隐藏浏览器
        /// </summary>
        public void SetVisible(bool visible)
        {
            if (_browserForm != null)
            {
                if (visible)
                {
                    _browserForm.Show();
                }
                else
                {
                    _browserForm.Hide();
                }
            }
        }
        
        /// <summary>
        /// 创建平台脚本
        /// </summary>
        private IPlatformScript CreateScript(PlatformType platform)
        {
            return platform switch
            {
                PlatformType.YunDing28 => new YunDing28Script(),
                PlatformType.BingGo28 => new BingGo28Script(),
                _ => throw new NotSupportedException($"不支持的平台: {platform}")
            };
        }
        
        public void Dispose()
        {
            _browser?.Dispose();
            _browserForm?.Close();
            _browserForm?.Dispose();
        }
    }
}
```

---

### 3️⃣ 平台脚本（简化接口）

```csharp
namespace BaiShengVx3Plus.Scripts
{
    /// <summary>
    /// 平台脚本接口 - 只需实现这几个方法
    /// </summary>
    public interface IPlatformScript
    {
        string PlatformUrl { get; }
        string GetInitScript();
        string GetLoginScript(string username, string password);
        string GetBalanceScript();
        string GetPlaceBetScript(BetOrder order);
    }
    
    /// <summary>
    /// 云顶28 脚本
    /// </summary>
    public class YunDing28Script : IPlatformScript
    {
        public string PlatformUrl => "https://www.yunding28.com";
        
        public string GetInitScript()
        {
            return @"
                console.log('云顶28 脚本已加载');
                window.bsAutoBet.platform = 'yunding28';
            ";
        }
        
        public string GetLoginScript(string username, string password)
        {
            return $@"
                (function() {{
                    document.querySelector('input[name=""username""]').value = '{username}';
                    document.querySelector('input[name=""password""]').value = '{password}';
                    document.querySelector('button.login-btn').click();
                }})();
            ";
        }
        
        public string GetBalanceScript()
        {
            return @"
                (function() {
                    var text = document.querySelector('.user-balance')?.textContent || '0';
                    return parseFloat(text.replace(/[^0-9.]/g, ''));
                })();
            ";
        }
        
        public string GetPlaceBetScript(BetOrder order)
        {
            return $@"
                (function() {{
                    // 1. 选择玩法
                    document.querySelector('[data-play=""{order.PlayType}""]').click();
                    
                    // 2. 填写金额
                    document.querySelector('input.bet-amount').value = {order.Amount};
                    
                    // 3. 选择号码
                    document.querySelector('[data-option=""{order.BetContent}""]').click();
                    
                    // 4. 确认投注
                    document.querySelector('button.confirm-bet').click();
                }})();
            ";
        }
    }
}
```

---

## 🎨 UI 集成（快速设置面板）

```csharp
// VxMain.cs 中添加

private AutoBetService? _autoBetService;

private async void BtnStartAutoBet_Click(object sender, EventArgs e)
{
    // 获取选择的平台
    var platform = (PlatformType)cmbPlatform.SelectedValue;
    
    // 启动浏览器
    _autoBetService = new AutoBetService(_logService);
    var success = await _autoBetService.StartAsync(platform);
    
    if (success)
    {
        MessageBox.Show("自动投注已启动！");
        
        // 是否显示浏览器窗口
        _autoBetService.ShowBrowser(chkShowBrowser.Checked);
    }
}

private async void BtnPlaceBet_Click(object sender, EventArgs e)
{
    if (_autoBetService == null)
    {
        MessageBox.Show("请先启动浏览器！");
        return;
    }
    
    // 创建订单
    var order = new BetOrder
    {
        PlayType = txtPlayType.Text,
        BetContent = txtBetContent.Text,
        Amount = decimal.Parse(txtAmount.Text)
    };
    
    // 投注
    var result = await _autoBetService.PlaceBetAsync(order);
    
    if (result.Success)
    {
        MessageBox.Show($"投注成功！订单号: {result.OrderId}");
    }
    else
    {
        MessageBox.Show($"投注失败: {result.ErrorMessage}");
    }
}
```

---

## 📊 扩展到多实例（未来）

### 当前设计（1 个浏览器）
```csharp
private AutoBetService _autoBetService;
```

### 扩展设计（多个浏览器，只需改一行）
```csharp
private Dictionary<string, AutoBetService> _autoBetServices;

// 启动多个浏览器
_autoBetServices["yunding28"] = new AutoBetService(logService);
await _autoBetServices["yunding28"].StartAsync(PlatformType.YunDing28);

_autoBetServices["binggo28"] = new AutoBetService(logService);
await _autoBetServices["binggo28"].StartAsync(PlatformType.BingGo28);

// 投注到不同浏览器
await _autoBetServices["yunding28"].PlaceBetAsync(order1);
await _autoBetServices["binggo28"].PlaceBetAsync(order2);
```

**扩展非常简单，不需要改架构！**

---

## ✅ 优势总结

| 对比项 | 复杂方案 | **简化方案** ✅ |
|-------|---------|----------------|
| **核心类数量** | 10+ 个 | **3 个** |
| **代码行数** | 2000+ 行 | **~500 行** |
| **通信机制** | WebSocket/IPC | **直接调用** |
| **独立进程** | 是 | **否（内嵌）** |
| **学习成本** | 高 | **低** |
| **维护难度** | 高 | **低** |
| **调试难度** | 高（跨进程） | **低（单进程）** |
| **扩展性** | 优秀 | **够用** |
| **当前需求** | 过度设计 | **刚刚好** |

---

## 🎯 实施步骤（简单）

### Week 1：基础搭建
1. ✅ 安装 CefSharp NuGet 包
2. ✅ 创建 `AutoBetService.cs`（100 行）
3. ✅ 创建 `BrowserManager.cs`（200 行）
4. ✅ 创建 `IPlatformScript.cs`（50 行）

### Week 2：平台脚本
1. ✅ 实现 `YunDing28Script.cs`（100 行）
2. ✅ 实现 `BingGo28Script.cs`（100 行）
3. ✅ 测试登录、获取余额、投注

### Week 3：UI 集成
1. ✅ 快速设置面板添加控件
2. ✅ 集成到 VxMain
3. ✅ 完整测试

**总共不到 1000 行代码，3 周完成！**

---

## 🎉 总结

### 简化方案的核心思想

1. **内嵌浏览器**：不需要独立进程
2. **直接调用**：不需要复杂通信
3. **3 个核心类**：AutoBetService + BrowserManager + PlatformScript
4. **够用就好**：先满足 1 个浏览器，需要时再扩展

### 代码量对比

| 方案 | 代码量 | 维护性 |
|------|-------|--------|
| WebSocket 方案 | 2000+ 行 | ⚠️ 复杂 |
| **简化方案** | **~500 行** | ✅ **简单** |

### 关键代码

```csharp
// 就这么简单！
var autoBet = new AutoBetService(logService);
await autoBet.StartAsync(PlatformType.YunDing28);
await autoBet.LoginAsync("username", "password");
var balance = await autoBet.GetBalanceAsync();
var result = await autoBet.PlaceBetAsync(order);
```

---

**这个简化方案：代码少、易理解、易维护、够用！** 

**您觉得这个方案如何？** 😊
