# 待删除 Service 清单

## ❌ 立即删除的文件

### Services 层

```
Services/
├── Member/
│   └── MemberService.cs (220行) - 已被 V2MemberBindingList 替代
├── Order/
│   └── OrderService.cs (300行) - 已被 V2OrderBindingList 替代
├── Database/
│   ├── DatabaseService.cs (400行) - ORM 自动建表，无需此服务
│   └── PropertyChangeTracker.cs (200行) - BindingList 自动追踪
└── Contact/
    └── ContactBindingService.cs (50行) - 只存一个变量，无需独立服务
```

### Contracts 层

```
Contracts/
├── IMemberService.cs - 对应 MemberService
├── IOrderService.cs - 对应 OrderService
├── IDatabaseService.cs - 对应 DatabaseService
├── IPropertyChangeTracker.cs - 对应 PropertyChangeTracker
└── IContactBindingService.cs - 对应 ContactBindingService
```

## ⚠️ 简化但保留的文件

```
Services/Contact/
└── ContactDataService.cs (300行 → 100行)
    - 保留 GetContacts() 数据处理逻辑
    - 删除数据库操作（改用 ORM）
```

---

## 📊 删除统计

| 类别       | 文件数 | 代码行数 |
|------------|--------|----------|
| Services   | 5      | ~1170    |
| Contracts  | 5      | ~150     |
| **总计**   | **10** | **~1320** |

---

**创建日期**: 2025-11-06  
**状态**: 准备删除

