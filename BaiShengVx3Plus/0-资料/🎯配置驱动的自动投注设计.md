# 配置驱动的自动投注设计 - 简洁、灵活、易扩展

## 🎯 核心思想

### 配置驱动 + ID 标识
1. **每个浏览器 = 一个配置**
2. **配置包含**：ID（唯一标识）、平台、账号、密码等
3. **所有操作带 ID**：投注订单、返回结果都标注配置 ID
4. **扩展简单**：新增浏览器 = 新增配置

### 默认配置
- 系统自带一个"默认配置"
- 在**快速设置**中直接修改默认配置
- 普通用户只需在快速设置中操作

### 配置管理器
- 高级功能：管理多个配置
- 可以添加、编辑、删除配置
- 每个配置独立设置平台、账号等

---

## 🏗️ 架构设计

```
┌──────────────────────────────────────────────────────────────┐
│                    BaiShengVx3Plus                           │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │         BetConfigManager (配置管理器)                  │ │
│  │  • Configs: List<BetConfig>                           │ │
│  │  • DefaultConfig (默认配置)                            │ │
│  │  • Add/Edit/Delete/Get                                │ │
│  └────────────────┬───────────────────────────────────────┘ │
│                   │                                          │
│  ┌────────────────▼───────────────────────────────────────┐ │
│  │         AutoBetService (自动投注服务)                  │ │
│  │  • Browsers: Dictionary<ConfigId, BrowserInstance>    │ │
│  │  • StartBrowser(config)                               │ │
│  │  • PlaceBet(configId, order)                          │ │
│  │  • StopBrowser(configId)                              │ │
│  └────────────────┬───────────────────────────────────────┘ │
│                   │                                          │
│  ┌────────────────▼───────────────────────────────────────┐ │
│  │      BrowserInstance (浏览器实例)                      │ │
│  │  • ConfigId: string                                   │ │
│  │  • Config: BetConfig                                  │ │
│  │  • Browser: ChromiumWebBrowser                        │ │
│  │  • PlatformScript: IPlatformScript                    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  UI:                                                         │
│  • 快速设置面板 (编辑默认配置)                              │
│  • 配置管理器窗口 (管理所有配置)                             │
└──────────────────────────────────────────────────────────────┘
```

---

## 📦 数据模型

### BetConfig（配置模型）

```csharp
namespace BaiShengVx3Plus.Models.AutoBet
{
    /// <summary>
    /// 自动投注配置
    /// </summary>
    [Table("AutoBetConfigs")]
    public class BetConfig : INotifyPropertyChanged
    {
        /// <summary>
        /// 配置 ID（唯一标识）
        /// </summary>
        [PrimaryKey, AutoIncrement]
        public int Id { get; set; }
        
        /// <summary>
        /// 配置名称（用户自定义）
        /// </summary>
        [DataGridColumn(HeaderText = "配置名称", Order = 1, Width = 150)]
        public string ConfigName { get; set; } = "";
        
        /// <summary>
        /// 平台类型
        /// </summary>
        [DataGridColumn(HeaderText = "平台", Order = 2, Width = 100)]
        public string Platform { get; set; } = "YunDing28";
        
        /// <summary>
        /// 平台 URL
        /// </summary>
        public string PlatformUrl { get; set; } = "";
        
        /// <summary>
        /// 登录账号
        /// </summary>
        [DataGridColumn(HeaderText = "账号", Order = 3, Width = 120)]
        public string Username { get; set; } = "";
        
        /// <summary>
        /// 登录密码（加密存储）
        /// </summary>
        public string Password { get; set; } = "";
        
        /// <summary>
        /// 是否启用
        /// </summary>
        [DataGridColumn(HeaderText = "启用", Order = 4, Width = 60)]
        public bool IsEnabled { get; set; } = true;
        
        /// <summary>
        /// 是否显示浏览器窗口
        /// </summary>
        [DataGridColumn(HeaderText = "显示窗口", Order = 5, Width = 80)]
        public bool ShowBrowserWindow { get; set; } = false;
        
        /// <summary>
        /// 最小投注金额
        /// </summary>
        public decimal MinBetAmount { get; set; } = 1;
        
        /// <summary>
        /// 最大投注金额
        /// </summary>
        public decimal MaxBetAmount { get; set; } = 10000;
        
        /// <summary>
        /// 是否自动登录
        /// </summary>
        public bool AutoLogin { get; set; } = true;
        
        /// <summary>
        /// 当前余额
        /// </summary>
        [DataGridColumn(HeaderText = "余额", Order = 6, Width = 100, Format = "F2")]
        public decimal CurrentBalance { get; set; }
        
        /// <summary>
        /// 状态
        /// </summary>
        [DataGridColumn(HeaderText = "状态", Order = 7, Width = 100)]
        public string Status { get; set; } = "未启动";
        
        /// <summary>
        /// 最后更新时间
        /// </summary>
        public DateTime LastUpdateTime { get; set; } = DateTime.Now;
        
        /// <summary>
        /// 备注
        /// </summary>
        public string? Notes { get; set; }
        
        /// <summary>
        /// 是否为默认配置
        /// </summary>
        public bool IsDefault { get; set; } = false;
        
        public event PropertyChangedEventHandler? PropertyChanged;
    }
}
```

### BetOrderRecord（投注记录，带配置 ID）

```csharp
namespace BaiShengVx3Plus.Models.AutoBet
{
    /// <summary>
    /// 自动投注订单记录
    /// </summary>
    [Table("AutoBetOrders")]
    public class BetOrderRecord
    {
        [PrimaryKey, AutoIncrement]
        public long Id { get; set; }
        
        /// <summary>
        /// 🔥 配置 ID（标识是哪个浏览器的订单）
        /// </summary>
        [DataGridColumn(HeaderText = "配置", Order = 1, Width = 120)]
        public string ConfigName { get; set; } = "";
        
        /// <summary>
        /// 配置 ID
        /// </summary>
        public int ConfigId { get; set; }
        
        /// <summary>
        /// 平台
        /// </summary>
        [DataGridColumn(HeaderText = "平台", Order = 2, Width = 100)]
        public string Platform { get; set; } = "";
        
        /// <summary>
        /// 期号
        /// </summary>
        [DataGridColumn(HeaderText = "期号", Order = 3, Width = 100)]
        public string IssueId { get; set; } = "";
        
        /// <summary>
        /// 玩法
        /// </summary>
        [DataGridColumn(HeaderText = "玩法", Order = 4, Width = 80)]
        public string PlayType { get; set; } = "";
        
        /// <summary>
        /// 投注内容
        /// </summary>
        [DataGridColumn(HeaderText = "内容", Order = 5, Width = 100)]
        public string BetContent { get; set; } = "";
        
        /// <summary>
        /// 金额
        /// </summary>
        [DataGridColumn(HeaderText = "金额", Order = 6, Width = 80, Format = "F2")]
        public decimal Amount { get; set; }
        
        /// <summary>
        /// 平台订单号
        /// </summary>
        [DataGridColumn(HeaderText = "平台订单号", Order = 7, Width = 150)]
        public string? PlatformOrderId { get; set; }
        
        /// <summary>
        /// 状态：成功/失败
        /// </summary>
        [DataGridColumn(HeaderText = "状态", Order = 8, Width = 80)]
        public string Status { get; set; } = "待处理";
        
        /// <summary>
        /// 错误信息
        /// </summary>
        [DataGridColumn(HeaderText = "备注", Order = 9, Width = 200)]
        public string? ErrorMessage { get; set; }
        
        /// <summary>
        /// 投注时间
        /// </summary>
        [DataGridColumn(HeaderText = "时间", Order = 10, Width = 150)]
        public DateTime CreateTime { get; set; } = DateTime.Now;
        
        /// <summary>
        /// 结算时间
        /// </summary>
        public DateTime? SettleTime { get; set; }
    }
}
```

---

## 💻 核心代码实现

### 1️⃣ BetConfigManager（配置管理器）

```csharp
namespace BaiShengVx3Plus.Services.AutoBet
{
    /// <summary>
    /// 自动投注配置管理器
    /// </summary>
    public class BetConfigManager
    {
        private readonly SQLiteConnection _db;
        private readonly ILogService _logService;
        
        public BetConfigManager(SQLiteConnection db, ILogService logService)
        {
            _db = db;
            _logService = logService;
            
            // 创建表
            _db.CreateTable<BetConfig>();
            
            // 确保有默认配置
            EnsureDefaultConfig();
        }
        
        /// <summary>
        /// 确保存在默认配置
        /// </summary>
        private void EnsureDefaultConfig()
        {
            var defaultConfig = _db.Table<BetConfig>()
                .Where(c => c.IsDefault)
                .FirstOrDefault();
            
            if (defaultConfig == null)
            {
                // 创建默认配置
                defaultConfig = new BetConfig
                {
                    ConfigName = "默认配置",
                    Platform = "YunDing28",
                    PlatformUrl = "https://www.yunding28.com",
                    IsDefault = true,
                    IsEnabled = true,
                    ShowBrowserWindow = false,
                    MinBetAmount = 1,
                    MaxBetAmount = 10000,
                    Status = "未启动"
                };
                
                _db.Insert(defaultConfig);
                _logService.Info("BetConfigManager", "✅ 已创建默认配置");
            }
        }
        
        /// <summary>
        /// 获取默认配置
        /// </summary>
        public BetConfig GetDefaultConfig()
        {
            return _db.Table<BetConfig>()
                .Where(c => c.IsDefault)
                .First();
        }
        
        /// <summary>
        /// 更新默认配置（快速设置使用）
        /// </summary>
        public void UpdateDefaultConfig(BetConfig config)
        {
            var defaultConfig = GetDefaultConfig();
            
            // 更新字段
            defaultConfig.Platform = config.Platform;
            defaultConfig.PlatformUrl = config.PlatformUrl;
            defaultConfig.Username = config.Username;
            defaultConfig.Password = config.Password;
            defaultConfig.ShowBrowserWindow = config.ShowBrowserWindow;
            defaultConfig.MinBetAmount = config.MinBetAmount;
            defaultConfig.MaxBetAmount = config.MaxBetAmount;
            defaultConfig.AutoLogin = config.AutoLogin;
            defaultConfig.LastUpdateTime = DateTime.Now;
            
            _db.Update(defaultConfig);
            _logService.Info("BetConfigManager", "✅ 已更新默认配置");
        }
        
        /// <summary>
        /// 获取所有配置
        /// </summary>
        public List<BetConfig> GetAllConfigs()
        {
            return _db.Table<BetConfig>()
                .OrderBy(c => c.IsDefault ? 0 : 1)  // 默认配置排在最前
                .ThenBy(c => c.Id)
                .ToList();
        }
        
        /// <summary>
        /// 获取配置
        /// </summary>
        public BetConfig? GetConfig(int configId)
        {
            return _db.Find<BetConfig>(configId);
        }
        
        /// <summary>
        /// 添加配置
        /// </summary>
        public int AddConfig(BetConfig config)
        {
            // 确保不是默认配置
            config.IsDefault = false;
            config.LastUpdateTime = DateTime.Now;
            
            _db.Insert(config);
            
            _logService.Info("BetConfigManager", $"➕ 添加配置: {config.ConfigName}");
            return config.Id;
        }
        
        /// <summary>
        /// 更新配置
        /// </summary>
        public void UpdateConfig(BetConfig config)
        {
            config.LastUpdateTime = DateTime.Now;
            _db.Update(config);
            
            _logService.Info("BetConfigManager", $"✏️ 更新配置: {config.ConfigName}");
        }
        
        /// <summary>
        /// 删除配置
        /// </summary>
        public bool DeleteConfig(int configId)
        {
            var config = GetConfig(configId);
            if (config == null)
                return false;
            
            // 不能删除默认配置
            if (config.IsDefault)
            {
                _logService.Warning("BetConfigManager", "❌ 不能删除默认配置");
                return false;
            }
            
            _db.Delete(config);
            _logService.Info("BetConfigManager", $"🗑️ 删除配置: {config.ConfigName}");
            return true;
        }
        
        /// <summary>
        /// 获取启用的配置
        /// </summary>
        public List<BetConfig> GetEnabledConfigs()
        {
            return _db.Table<BetConfig>()
                .Where(c => c.IsEnabled)
                .ToList();
        }
        
        /// <summary>
        /// 更新配置状态
        /// </summary>
        public void UpdateConfigStatus(int configId, string status)
        {
            var config = GetConfig(configId);
            if (config != null)
            {
                config.Status = status;
                config.LastUpdateTime = DateTime.Now;
                _db.Update(config);
            }
        }
        
        /// <summary>
        /// 更新配置余额
        /// </summary>
        public void UpdateConfigBalance(int configId, decimal balance)
        {
            var config = GetConfig(configId);
            if (config != null)
            {
                config.CurrentBalance = balance;
                config.LastUpdateTime = DateTime.Now;
                _db.Update(config);
            }
        }
    }
}
```

---

### 2️⃣ AutoBetService（重构，支持多配置）

```csharp
namespace BaiShengVx3Plus.Services.AutoBet
{
    /// <summary>
    /// 自动投注服务
    /// </summary>
    public class AutoBetService
    {
        private readonly Dictionary<int, BrowserInstance> _browsers;
        private readonly BetConfigManager _configManager;
        private readonly ILogService _logService;
        private readonly SQLiteConnection _db;
        
        public AutoBetService(
            BetConfigManager configManager, 
            ILogService logService,
            SQLiteConnection db)
        {
            _browsers = new Dictionary<int, BrowserInstance>();
            _configManager = configManager;
            _logService = logService;
            _db = db;
            
            // 创建订单表
            _db.CreateTable<BetOrderRecord>();
        }
        
        /// <summary>
        /// 🔥 启动浏览器（根据配置 ID）
        /// </summary>
        public async Task<bool> StartBrowserAsync(int configId)
        {
            try
            {
                // 获取配置
                var config = _configManager.GetConfig(configId);
                if (config == null)
                {
                    _logService.Error("AutoBet", $"配置不存在: {configId}");
                    return false;
                }
                
                // 检查是否已启动
                if (_browsers.ContainsKey(configId))
                {
                    _logService.Warning("AutoBet", $"浏览器已启动: {config.ConfigName}");
                    return true;
                }
                
                _logService.Info("AutoBet", $"🚀 启动浏览器: {config.ConfigName} - {config.Platform}");
                
                // 创建浏览器实例
                var browser = new BrowserInstance(config, _logService);
                await browser.InitializeAsync();
                
                // 保存实例
                _browsers[configId] = browser;
                
                // 更新配置状态
                _configManager.UpdateConfigStatus(configId, "已启动");
                
                // 自动登录
                if (config.AutoLogin && !string.IsNullOrEmpty(config.Username))
                {
                    await LoginAsync(configId, config.Username, config.Password);
                }
                
                // 显示/隐藏窗口
                browser.SetVisible(config.ShowBrowserWindow);
                
                _logService.Info("AutoBet", $"✅ 浏览器启动成功: {config.ConfigName}");
                return true;
            }
            catch (Exception ex)
            {
                _logService.Error("AutoBet", $"启动浏览器失败: {configId}", ex);
                _configManager.UpdateConfigStatus(configId, "启动失败");
                return false;
            }
        }
        
        /// <summary>
        /// 🔥 停止浏览器
        /// </summary>
        public void StopBrowser(int configId)
        {
            if (_browsers.TryGetValue(configId, out var browser))
            {
                browser.Dispose();
                _browsers.Remove(configId);
                _configManager.UpdateConfigStatus(configId, "已停止");
                
                var config = _configManager.GetConfig(configId);
                _logService.Info("AutoBet", $"⏹️ 浏览器已停止: {config?.ConfigName}");
            }
        }
        
        /// <summary>
        /// 🔥 登录
        /// </summary>
        public async Task<bool> LoginAsync(int configId, string username, string password)
        {
            if (!_browsers.TryGetValue(configId, out var browser))
            {
                _logService.Warning("AutoBet", "浏览器未启动");
                return false;
            }
            
            var success = await browser.ExecuteLoginAsync(username, password);
            
            if (success)
            {
                _configManager.UpdateConfigStatus(configId, "已登录");
                
                // 获取余额
                var balance = await browser.ExecuteGetBalanceAsync();
                _configManager.UpdateConfigBalance(configId, balance);
            }
            else
            {
                _configManager.UpdateConfigStatus(configId, "登录失败");
            }
            
            return success;
        }
        
        /// <summary>
        /// 🔥 投注（带配置 ID）
        /// </summary>
        public async Task<BetResult> PlaceBetAsync(int configId, BetOrder order)
        {
            if (!_browsers.TryGetValue(configId, out var browser))
            {
                return new BetResult 
                { 
                    Success = false, 
                    ErrorMessage = "浏览器未启动" 
                };
            }
            
            var config = _configManager.GetConfig(configId);
            if (config == null)
            {
                return new BetResult 
                { 
                    Success = false, 
                    ErrorMessage = "配置不存在" 
                };
            }
            
            _logService.Info("AutoBet", 
                $"📤 [{config.ConfigName}] 投注: {order.PlayType} {order.BetContent} {order.Amount}");
            
            // 执行投注
            var result = await browser.ExecutePlaceBetAsync(order);
            
            // 🔥 保存投注记录（带配置信息）
            var record = new BetOrderRecord
            {
                ConfigId = configId,
                ConfigName = config.ConfigName,
                Platform = config.Platform,
                IssueId = order.IssueId ?? "",
                PlayType = order.PlayType,
                BetContent = order.BetContent,
                Amount = order.Amount,
                PlatformOrderId = result.OrderId,
                Status = result.Success ? "成功" : "失败",
                ErrorMessage = result.ErrorMessage,
                CreateTime = DateTime.Now
            };
            
            _db.Insert(record);
            
            // 更新余额
            if (result.Success)
            {
                var balance = await browser.ExecuteGetBalanceAsync();
                _configManager.UpdateConfigBalance(configId, balance);
            }
            
            _logService.Info("AutoBet", 
                $"📥 [{config.ConfigName}] 投注结果: {(result.Success ? "✅ 成功" : "❌ 失败")}");
            
            return result;
        }
        
        /// <summary>
        /// 获取余额
        /// </summary>
        public async Task<decimal> GetBalanceAsync(int configId)
        {
            if (!_browsers.TryGetValue(configId, out var browser))
                return 0;
            
            var balance = await browser.ExecuteGetBalanceAsync();
            _configManager.UpdateConfigBalance(configId, balance);
            return balance;
        }
        
        /// <summary>
        /// 🔥 启动所有启用的浏览器
        /// </summary>
        public async Task StartAllEnabledBrowsersAsync()
        {
            var enabledConfigs = _configManager.GetEnabledConfigs();
            
            foreach (var config in enabledConfigs)
            {
                await StartBrowserAsync(config.Id);
            }
        }
        
        /// <summary>
        /// 停止所有浏览器
        /// </summary>
        public void StopAllBrowsers()
        {
            foreach (var configId in _browsers.Keys.ToList())
            {
                StopBrowser(configId);
            }
        }
        
        /// <summary>
        /// 获取运行中的浏览器列表
        /// </summary>
        public List<BrowserInfo> GetRunningBrowsers()
        {
            return _browsers.Select(kvp =>
            {
                var config = _configManager.GetConfig(kvp.Key);
                return new BrowserInfo
                {
                    ConfigId = kvp.Key,
                    ConfigName = config?.ConfigName ?? "",
                    Platform = config?.Platform ?? "",
                    Status = config?.Status ?? ""
                };
            }).ToList();
        }
    }
    
    /// <summary>
    /// 浏览器信息
    /// </summary>
    public class BrowserInfo
    {
        public int ConfigId { get; set; }
        public string ConfigName { get; set; } = "";
        public string Platform { get; set; } = "";
        public string Status { get; set; } = "";
    }
}
```

---

## 🎨 UI 设计

### 1️⃣ 快速设置面板（编辑默认配置）

```
┌─────────────────────────────────────────┐
│     快速设置 - 自动投注 (默认配置)      │
├─────────────────────────────────────────┤
│                                         │
│  平台: [下拉选择: 云顶28 ▼]             │
│                                         │
│  账号: [_________________]              │
│                                         │
│  密码: [_________________]              │
│                                         │
│  [ ] 显示浏览器窗口                      │
│  [✓] 自动登录                           │
│                                         │
│  投注限额:                               │
│    最小金额: [____]  最大金额: [_____]  │
│                                         │
│  [保存配置]  [启动浏览器]  [配置管理器]  │
│                                         │
└─────────────────────────────────────────┘
```

### 2️⃣ 配置管理器窗口（已实现）

**文件位置**: 
- `BaiShengVx3Plus/Views/AutoBet/BetConfigManagerForm.cs`
- `BaiShengVx3Plus/Views/AutoBet/BetConfigManagerForm.Designer.cs`

**布局结构**:

```
┌──────────────────────────────────────────────────────────────┐
│              自动投注配置管理器 (1400×800)                    │
├──────────────────────────────────────────────────────────────┤
│ 顶部工具栏 (高60px)                                          │
│ [➕ 新增配置] [✏️ 编辑] [🗑️ 删除] [🔄 刷新]                 │
│ [▶ 启动全部] [⏹️ 停止全部]                                  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│ 上半部分: 配置列表 (SplitContainer.Panel1 - 高250px)        │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ DataGridView - 所有配置                                 │  │
│ │ ID │配置名称    │平台   │账号     │启用│显示窗口│状态  │  │
│ │ ──┼──────────┼───────┼────────┼────┼────────┼───────│  │
│ │ 1 │默认配置   │云顶28 │test001 │✓  │        │已登录 │  │
│ │ 2 │炳狗账号1  │炳狗28 │user001 │✓  │✓      │未启动 │  │
│ │ 3 │快乐8账号  │快乐8  │kl8user │   │        │已停止 │  │
│ └────────────────────────────────────────────────────────┘  │
│ 共 3 个配置                                                   │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│ 下半部分: 配置详情 (SplitContainer.Panel2)                  │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ [基本设置] [高级设置] [投注记录]  (TabControl)          │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │                                                         │  │
│ │ 【基本设置】选项卡:                                      │  │
│ │                                                         │  │
│ │ 左侧表单区:                                              │  │
│ │   配置名称: [_________________________]                 │  │
│ │   游戏平台: [云顶28 ▼]                                  │  │
│ │   平台网址: [https://www.yunding28.com___________]     │  │
│ │   登录账号: [_________________________]                 │  │
│ │   登录密码: [●●●●●●●●●●]  [ ] 显示密码                 │  │
│ │                                                         │  │
│ │   [✓] 启用此配置  [✓] 自动登录  [ ] 显示浏览器窗口     │  │
│ │                                                         │  │
│ │ 右侧状态区:                                              │  │
│ │ ┌─────────── 状态信息 ───────────┐                      │  │
│ │ │ 状态: 已登录 (绿色)              │                      │  │
│ │ │ 余额: 5,328.50 (绿色加粗)        │                      │  │
│ │ │ 最后更新: 2025-11-07 14:25:30   │                      │  │
│ │ │              [刷新余额]          │                      │  │
│ │ └─────────────────────────────────┘                      │  │
│ │                                                         │  │
│ │ 底部操作按钮:                                            │  │
│ │ [💾 保存配置] [🔧 测试连接] [▶ 启动浏览器] [⏹ 停止]    │  │
│ │                                                         │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │                                                         │  │
│ │ 【高级设置】选项卡:                                      │  │
│ │                                                         │  │
│ │   最小投注额: [____1.00____]                            │  │
│ │   最大投注额: [___10000.00_]                            │  │
│ │                                                         │  │
│ │   备注: ┌───────────────────────────────────┐          │  │
│ │        │ 可以输入一些备注信息...             │          │  │
│ │        │                                     │          │  │
│ │        └───────────────────────────────────┘          │  │
│ │                                                         │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │                                                         │  │
│ │ 【投注记录】选项卡:                                      │  │
│ │                                                         │  │
│ │ 时间范围: [2025-11-07▼] 至 [2025-11-07▼] [🔍 筛选]    │  │
│ │                                                         │  │
│ │ ┌──────────────────────────────────────────────────┐  │  │
│ │ │ DataGridView - 该配置的投注记录                   │  │  │
│ │ │ 配置  │期号    │玩法│内容│金额│订单号   │状态│时间│  │  │
│ │ │ ─────┼────────┼────┼────┼────┼─────────┼────┼────│  │  │
│ │ │ 默认  │11406294│大小│大  │100 │ORD12345 │成功│14:30│  │  │
│ │ │ 默认  │11406293│单双│单  │50  │ORD12346 │成功│14:28│  │  │
│ │ │ 默认  │11406292│特码│5   │200 │ORD12347 │失败│14:25│  │  │
│ │ └──────────────────────────────────────────────────┘  │  │
│ │                                                         │  │
│ │ 共 125 条记录 | 成功: 120 | 失败: 5 | 总金额: 6,580.00  │  │
│ │                                                         │  │
│ └────────────────────────────────────────────────────────┘  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**功能特性**:

1. **上下分割布局** (SplitContainer)
   - 上半部分：配置列表（可快速切换）
   - 下半部分：详细设置（三个选项卡）

2. **三个选项卡**
   - **基本设置**: 账号密码、状态显示、启动/停止
   - **高级设置**: 投注限额、备注
   - **投注记录**: 该配置的历史订单

3. **实时状态显示**
   - 状态（颜色标识）: 已登录（绿）、已启动（蓝）、失败（红）
   - 余额显示（绿色加粗）
   - 最后更新时间

4. **完整操作**
   - 新增、编辑、删除配置
   - 保存、测试连接
   - 启动/停止单个或全部浏览器
   - 刷新余额

5. **数据绑定**
   - `BindingList<BetConfig>` → 配置列表
   - `BindingList<BetOrderRecord>` → 投注记录
   - 实时同步到数据库

---

## 📝 使用示例

### 场景1：普通用户（只用默认配置）

```csharp
// 在快速设置面板中操作

// 1. 修改默认配置
var defaultConfig = _configManager.GetDefaultConfig();
defaultConfig.Platform = "YunDing28";
defaultConfig.Username = "test001";
defaultConfig.Password = "aaa111";
_configManager.UpdateDefaultConfig(defaultConfig);

// 2. 启动默认浏览器
await _autoBetService.StartBrowserAsync(defaultConfig.Id);

// 3. 投注（自动使用默认配置）
var order = new BetOrder { PlayType = "大小", BetContent = "大", Amount = 100 };
var result = await _autoBetService.PlaceBetAsync(defaultConfig.Id, order);
```

### 场景2：高级用户（多配置）

```csharp
// 在配置管理器中操作

// 1. 添加云顶28配置
var config1 = new BetConfig
{
    ConfigName = "云顶账号1",
    Platform = "YunDing28",
    Username = "yd_user1",
    Password = "pass123",
    IsEnabled = true
};
var configId1 = _configManager.AddConfig(config1);

// 2. 添加炳狗28配置
var config2 = new BetConfig
{
    ConfigName = "炳狗账号1",
    Platform = "BingGo28",
    Username = "bg_user1",
    Password = "pass456",
    IsEnabled = true
};
var configId2 = _configManager.AddConfig(config2);

// 3. 启动多个浏览器
await _autoBetService.StartBrowserAsync(configId1);
await _autoBetService.StartBrowserAsync(configId2);

// 4. 向不同浏览器投注
var order1 = new BetOrder { PlayType = "大小", BetContent = "大", Amount = 100 };
await _autoBetService.PlaceBetAsync(configId1, order1);  // 云顶28

var order2 = new BetOrder { PlayType = "单双", BetContent = "单", Amount = 50 };
await _autoBetService.PlaceBetAsync(configId2, order2);  // 炳狗28

// 5. 查看投注记录（带配置标识）
var records = _db.Table<BetOrderRecord>()
    .Where(r => r.ConfigId == configId1)
    .ToList();
// 结果: [云顶账号1] 大小 大 100 成功
```

### 场景3：批量操作

```csharp
// 启动所有启用的浏览器
await _autoBetService.StartAllEnabledBrowsersAsync();

// 获取运行中的浏览器
var runningBrowsers = _autoBetService.GetRunningBrowsers();
foreach (var browser in runningBrowsers)
{
    Console.WriteLine($"{browser.ConfigName} - {browser.Platform} - {browser.Status}");
}

// 停止所有浏览器
_autoBetService.StopAllBrowsers();
```

---

## ✅ 核心优势

### 1️⃣ 配置驱动
- ✅ 每个浏览器 = 一个配置
- ✅ 配置独立存储（数据库）
- ✅ 所有操作带配置 ID

### 2️⃣ 易于扩展
```csharp
// 新增浏览器 = 新增配置
var newConfig = new BetConfig { ... };
var configId = _configManager.AddConfig(newConfig);
await _autoBetService.StartBrowserAsync(configId);
```

### 3️⃣ 默认配置
- ✅ 系统自带默认配置
- ✅ 快速设置直接修改
- ✅ 普通用户无需配置管理器

### 4️⃣ 订单追溯
```csharp
// 每个订单都标注配置信息
record.ConfigId = configId;
record.ConfigName = config.ConfigName;
record.Platform = config.Platform;

// 查询某个配置的所有订单
var orders = _db.Table<BetOrderRecord>()
    .Where(r => r.ConfigId == configId)
    .ToList();
```

---

## 📊 数据流图

```
用户操作
   │
   ├─► 快速设置面板
   │      │
   │      ├─► 修改默认配置 → BetConfigManager.UpdateDefaultConfig()
   │      └─► 启动浏览器 → AutoBetService.StartBrowserAsync(defaultConfigId)
   │
   └─► 配置管理器
          │
          ├─► 添加配置 → BetConfigManager.AddConfig()
          ├─► 编辑配置 → BetConfigManager.UpdateConfig()
          ├─► 删除配置 → BetConfigManager.DeleteConfig()
          └─► 启动浏览器 → AutoBetService.StartBrowserAsync(configId)

投注流程
   │
   ├─► AutoBetService.PlaceBetAsync(configId, order)
   │      │
   │      ├─► 查找浏览器实例: _browsers[configId]
   │      ├─► 执行投注: browser.ExecutePlaceBetAsync(order)
   │      ├─► 保存记录: BetOrderRecord { ConfigId, ConfigName, ... }
   │      └─► 更新余额: _configManager.UpdateConfigBalance(configId, balance)
   │
   └─► 结果返回（带配置信息）
```

---

## 🎉 总结

### 核心设计
1. **配置驱动**：每个浏览器对应一个配置
2. **ID 标识**：所有操作和记录都带配置 ID
3. **默认配置**：快速设置直接修改
4. **配置管理器**：高级功能，管理多配置

### 关键类
- `BetConfig`：配置模型（数据库表）
- `BetConfigManager`：配置管理器
- `AutoBetService`：自动投注服务（支持多配置）
- `BetOrderRecord`：订单记录（带配置 ID）

### 扩展能力
```csharp
// 扩展非常简单
1. 新增配置
2. 启动浏览器
3. 投注时指定配置 ID
```

---

**这个方案是否符合您的需求？** 🎯

- ✅ 配置驱动
- ✅ ID 标识
- ✅ 默认配置（快速设置）
- ✅ 配置管理器（高级功能）
- ✅ 易于扩展

如果确认，我们可以开始实施了！ 🚀

