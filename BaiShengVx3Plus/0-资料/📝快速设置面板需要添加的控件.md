# 📝 快速设置面板需要添加的控件

## 需要添加的自动投注配置项

在 `pnl_fastsetting` 面板中需要添加以下控件：

### 1. 盘口平台选择
```csharp
// ComboBox - 平台选择
private Label lblPlatform;
private UIComboBox cbxPlatform;  // 云顶28、海峡28 等
```

### 2. 账号配置
```csharp
// TextBox - 用户名
private Label lblUsername;
private UITextBox txtUsername;

// TextBox - 密码  
private Label lblPassword;
private UITextBox txtPassword;
```

### 3. 自动投注开关
```csharp
// CheckBox - 启用自动投注
private UICheckBox chkAutoB et;

// Button - 手动启动浏览器
private UIButton btnStartBrowser;
```

## 布局建议

```
┌─────────────────────────┐
│      快速设置            │
├─────────────────────────┤
│ 盘口: [云顶28 ▼]         │
│ 账号: [test001____]      │
│ 密码: [********___]      │
│ [√] 启用自动投注          │
│ [启动浏览器]             │
│                         │
│ 封盘: [30秒]            │
│ 最小: [1元]             │
│ 最大: [10000元]         │
└─────────────────────────┘
```

## 实现方式

由于修改 Designer 文件复杂，建议在 `VxMain.cs` 的 `InitializeComponent()` 之后动态添加控件。

见 `VxMain.cs` 中的 `InitializeAutoBetControls()` 方法。

