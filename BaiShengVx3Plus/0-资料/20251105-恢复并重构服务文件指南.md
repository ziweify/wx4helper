# 恢复并重构服务文件 - 操作指南

## 📅 日期
2025-11-05

## 🎯 目标
从 Git 历史中恢复被删除的服务文件，然后移动到新的文件夹结构并更新命名空间。

## ⚠️ 问题
在重构过程中，部分服务文件被删除但新文件没有正确创建，导致以下文件丢失：
- `ContactDataService.cs`
- `UserInfoService.cs`
- `WeChatService.cs`
- `WeChatLoaderService.cs`
- `WeixinSocketClient.cs`

## 🔄 解决方案

### 方法1：使用 Git 命令行（推荐）

打开 **Git Bash** 或 **命令提示符**，执行以下命令：

```bash
# 1. 进入项目目录
cd D:\gitcode\wx4helper

# 2. 查看最近删除的文件历史（可选）
git log --diff-filter=D --summary --oneline -20

# 3. 恢复被删除的文件到 Services 根目录
git checkout HEAD~1 -- BaiShengVx3Plus/Services/ContactDataService.cs
git checkout HEAD~1 -- BaiShengVx3Plus/Services/UserInfoService.cs  
git checkout HEAD~1 -- BaiShengVx3Plus/Services/WeChatService.cs
git checkout HEAD~1 -- BaiShengVx3Plus/Services/WeChatLoaderService.cs
git checkout HEAD~1 -- BaiShengVx3Plus/Services/WeixinSocketClient.cs

echo 文件已恢复到 BaiShengVx3Plus/Services/ 目录
```

### 方法2：使用 Visual Studio Git 功能

1. 在 Visual Studio 中打开 **"Git 更改"** 窗口
2. 点击 **"查看历史记录"**
3. 找到删除这些文件之前的提交（通常是最近的几个提交）
4. 右键点击该提交
5. 选择 **"签出"** 或 **"查看提交详细信息"**
6. 在文件列表中找到这些被删除的文件
7. 右键点击文件 → **"还原文件"**

### 方法3：使用批处理脚本

运行项目中的 `BaiShengVx3Plus/恢复服务文件.bat` 脚本。

---

## 📝 恢复后的重构步骤

### 步骤1：移动并重命名文件

恢复文件后，它们会在 `BaiShengVx3Plus/Services/` 根目录。需要移动到子文件夹：

```plaintext
移动前位置                                    → 移动后位置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Services/ContactDataService.cs              → Services/Contact/ContactDataService.cs
Services/UserInfoService.cs                 → Services/UserInfo/UserInfoService.cs
Services/WeChatService.cs                   → Services/WeChat/WeChatService.cs
Services/WeChatLoaderService.cs             → Services/WeChat/WeChatLoaderService.cs
Services/WeixinSocketClient.cs              → Services/WeChat/WeixinSocketClient.cs
```

### 步骤2：更新命名空间

打开每个文件，修改命名空间：

#### ContactDataService.cs
```csharp
// 修改前
namespace BaiShengVx3Plus.Services

// 修改后
namespace BaiShengVx3Plus.Services.Contact
```

添加 using：
```csharp
using BaiShengVx3Plus.Contracts;
```

#### UserInfoService.cs
```csharp
// 修改前
namespace BaiShengVx3Plus.Services

// 修改后
namespace BaiShengVx3Plus.Services.UserInfo
```

添加 using：
```csharp
using BaiShengVx3Plus.Contracts;
```

#### WeChatService.cs, WeChatLoaderService.cs, WeixinSocketClient.cs
```csharp
// 修改前
namespace BaiShengVx3Plus.Services

// 修改后
namespace BaiShengVx3Plus.Services.WeChat
```

添加 using：
```csharp
using BaiShengVx3Plus.Contracts;
```

### 步骤3：在 Visual Studio 中操作

1. **关闭 Visual Studio**
2. **移动文件到对应的子文件夹**（使用文件资源管理器）
3. **打开 Visual Studio**
4. **重新加载项目**
5. **检查所有文件是否正确显示**
6. **编译项目** (`Ctrl+Shift+B`)

---

## 🎯 最终的文件结构

```
BaiShengVx3Plus/
  ├─ Contracts/                   ← 所有接口
  │   ├─ IContactDataService.cs
  │   ├─ IUserInfoService.cs
  │   ├─ IWeChatService.cs
  │   ├─ IWeChatLoaderService.cs
  │   └─ IWeixinSocketClient.cs
  │
  └─ Services/                    ← 实现按功能分组
      ├─ Contact/
      │   └─ ContactDataService.cs  ← 从 git 恢复 + 重构
      ├─ UserInfo/
      │   └─ UserInfoService.cs     ← 从 git 恢复 + 重构
      └─ WeChat/
          ├─ WeChatService.cs       ← 从 git 恢复 + 重构
          ├─ WeChatLoaderService.cs ← 从 git 恢复 + 重构
          └─ WeixinSocketClient.cs  ← 从 git 恢复 + 重构
```

---

## ✅ 验证清单

- [ ] 所有文件已从 git 恢复
- [ ] 文件已移动到正确的子文件夹
- [ ] 命名空间已更新
- [ ] `using BaiShengVx3Plus.Contracts;` 已添加
- [ ] Visual Studio 项目已重新加载
- [ ] 项目编译成功（0 个错误）
- [ ] 所有功能测试通过

---

## 🚨 如果遇到问题

### 问题1：git checkout 提示"路径不存在"
**解决**：可能文件在更早的提交中，尝试：
```bash
git checkout HEAD~2 -- BaiShengVx3Plus/Services/ContactDataService.cs
# 或
git checkout HEAD~3 -- ...
```

### 问题2：Visual Studio 看不到新文件
**解决**：
1. 点击"显示所有文件"按钮
2. 右键新文件夹 → "包括在项目中"

### 问题3：命名空间冲突
**解决**：
- 确保已删除 `Services/` 根目录的旧文件
- 确保新文件的命名空间正确

---

## 📚 相关文档
- [20251105-项目结构重构指南.md](./20251105-项目结构重构指南.md)
- [20251105-重构错误修复记录.md](./20251105-重构错误修复记录.md)

