# ⭐ 极简实用版 - 最终方案

## 💡 设计原则

### 不要过度设计！
- ❌ 不要为了设计而设计
- ✅ **只解决实际问题**
- ✅ **能用最简单的方法就用最简单的**
- ✅ **代码少就是好代码**

---

## 🎯 实际需求分析

### 用户真正需要什么？

1. ✅ **保存多个打单配置**（平台、账号、密码）
2. ✅ **启动浏览器并自动登录**
3. ✅ **发送投注订单到浏览器**
4. ✅ **获取投注结果**
5. ✅ **配置可以随时修改**

### 不需要什么？

- ❌ 复杂的工厂模式（增加理解成本）
- ❌ 过多的抽象层（没有实际意义）
- ❌ 复杂的状态管理（简单的 string 就够了）
- ❌ 过度的设计模式（KISS 原则）

---

## 📦 最简架构

```
AutoBetService（唯一类）
    │
    ├─ Configs: List<BetConfig>              // 配置列表
    ├─ Browsers: Dictionary<int, Browser>    // 配置ID → 浏览器
    │
    ├─ LoadConfigs()                         // 加载配置
    ├─ SaveConfig(config)                    // 保存配置
    ├─ StartBrowser(configId)                // 启动浏览器
    ├─ PlaceBet(configId, order)             // 投注
    └─ StopBrowser(configId)                 // 停止浏览器
```

**就这么简单！一个类搞定！**

---

## 💻 核心代码（极简版）

### AutoBetService.cs（唯一核心类，~300 行）

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Windows.Forms;
using CefSharp;
using CefSharp.WinForms;
using SQLite;
using BaiShengVx3Plus.Models.AutoBet;
using BaiShengVx3Plus.Services;

namespace BaiShengVx3Plus.Services.AutoBet
{
    /// <summary>
    /// 自动投注服务 - 极简版，一个类搞定所有
    /// </summary>
    public class AutoBetService
    {
        private readonly SQLiteConnection _db;
        private readonly ILogService _log;
        
        // 🔥 核心数据：配置ID → 浏览器实例
        private readonly Dictionary<int, Browser> _browsers = new();
        
        public AutoBetService(SQLiteConnection db, ILogService log)
        {
            _db = db;
            _log = log;
            _db.CreateTable<BetConfig>();
            _db.CreateTable<BetOrderRecord>();
            EnsureDefaultConfig();
        }
        
        #region 配置管理（就这几个方法）
        
        public List<BetConfig> GetConfigs() => 
            _db.Table<BetConfig>().OrderBy(c => c.Id).ToList();
        
        public BetConfig GetConfig(int id) => 
            _db.Find<BetConfig>(id);
        
        public void SaveConfig(BetConfig config)
        {
            if (config.Id > 0)
                _db.Update(config);
            else
                _db.Insert(config);
        }
        
        public void DeleteConfig(int id)
        {
            StopBrowser(id);
            _db.Delete<BetConfig>(id);
        }
        
        #endregion
        
        #region 浏览器控制（核心功能）
        
        /// <summary>
        /// 🔥 启动浏览器
        /// </summary>
        public async Task<bool> StartBrowser(int configId)
        {
            try
            {
                var config = GetConfig(configId);
                if (config == null) return false;
                
                // 已启动就返回
                if (_browsers.ContainsKey(configId)) return true;
                
                _log.Info("AutoBet", $"启动浏览器: {config.ConfigName}");
                
                // 🔥 创建浏览器实例
                var browser = new Browser(config, _log);
                await browser.Start();
                
                _browsers[configId] = browser;
                
                // 自动登录
                if (config.AutoLogin && !string.IsNullOrEmpty(config.Username))
                {
                    await browser.Login(config.Username, config.Password);
                }
                
                return true;
            }
            catch (Exception ex)
            {
                _log.Error("AutoBet", "启动失败", ex);
                return false;
            }
        }
        
        /// <summary>
        /// 🔥 投注
        /// </summary>
        public async Task<BetResult> PlaceBet(int configId, BetOrder order)
        {
            if (!_browsers.TryGetValue(configId, out var browser))
                return new BetResult { Success = false, ErrorMessage = "浏览器未启动" };
            
            var result = await browser.PlaceBet(order);
            
            // 保存订单记录
            var config = GetConfig(configId);
            _db.Insert(new BetOrderRecord
            {
                ConfigId = configId,
                ConfigName = config.ConfigName,
                Platform = config.Platform,
                IssueId = order.IssueId,
                PlayType = order.PlayType,
                BetContent = order.BetContent,
                Amount = order.Amount,
                Status = result.Success ? "成功" : "失败",
                CreateTime = DateTime.Now
            });
            
            return result;
        }
        
        /// <summary>
        /// 停止浏览器
        /// </summary>
        public void StopBrowser(int configId)
        {
            if (_browsers.TryGetValue(configId, out var browser))
            {
                browser.Stop();
                _browsers.Remove(configId);
            }
        }
        
        #endregion
        
        #region 辅助方法
        
        private void EnsureDefaultConfig()
        {
            if (!_db.Table<BetConfig>().Any(c => c.IsDefault))
            {
                _db.Insert(new BetConfig
                {
                    ConfigName = "默认配置",
                    Platform = "YunDing28",
                    PlatformUrl = "https://www.yunding28.com",
                    IsDefault = true,
                    IsEnabled = true
                });
            }
        }
        
        #endregion
    }
}
```

---

### Browser.cs（浏览器封装，~200 行）

```csharp
namespace BaiShengVx3Plus.Services.AutoBet
{
    /// <summary>
    /// 浏览器实例 - 简单封装 CEF
    /// </summary>
    public class Browser
    {
        private readonly BetConfig _config;
        private readonly ILogService _log;
        private ChromiumWebBrowser _browser;
        private Form _form;
        
        public Browser(BetConfig config, ILogService log)
        {
            _config = config;
            _log = log;
        }
        
        /// <summary>
        /// 启动浏览器
        /// </summary>
        public async Task Start()
        {
            // 初始化 CEF
            if (!Cef.IsInitialized)
            {
                Cef.Initialize(new CefSettings
                {
                    CachePath = Path.Combine(
                        Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
                        "BaiShengBrowser", $"Config_{_config.Id}")
                });
            }
            
            // 创建浏览器
            _browser = new ChromiumWebBrowser(_config.PlatformUrl);
            
            // 创建窗体（如果需要显示）
            if (_config.ShowBrowserWindow)
            {
                _form = new Form
                {
                    Text = _config.ConfigName,
                    Width = 1280,
                    Height = 800
                };
                _form.Controls.Add(_browser);
                _form.Show();
            }
            
            // 等待加载
            await WaitForLoad();
        }
        
        /// <summary>
        /// 登录
        /// </summary>
        public async Task<bool> Login(string username, string password)
        {
            // 🔥 根据平台执行不同的登录脚本
            var script = GetLoginScript(username, password);
            await _browser.EvaluateScriptAsync(script);
            await Task.Delay(2000);
            return true;
        }
        
        /// <summary>
        /// 投注
        /// </summary>
        public async Task<BetResult> PlaceBet(BetOrder order)
        {
            try
            {
                // 🔥 根据平台执行不同的投注脚本
                var script = GetBetScript(order);
                await _browser.EvaluateScriptAsync(script);
                await Task.Delay(1000);
                
                return new BetResult { Success = true };
            }
            catch (Exception ex)
            {
                _log.Error("Browser", "投注失败", ex);
                return new BetResult { Success = false, ErrorMessage = ex.Message };
            }
        }
        
        /// <summary>
        /// 停止
        /// </summary>
        public void Stop()
        {
            _browser?.Dispose();
            _form?.Close();
        }
        
        #region 平台脚本（简单 switch，不需要工厂）
        
        private string GetLoginScript(string username, string password)
        {
            // 🔥 简单粗暴，直接 switch
            return _config.Platform switch
            {
                "YunDing28" => $@"
                    document.querySelector('input[name=""username""]').value = '{username}';
                    document.querySelector('input[name=""password""]').value = '{password}';
                    document.querySelector('button.login-btn').click();
                ",
                
                "BingGo28" => $@"
                    document.querySelector('#username').value = '{username}';
                    document.querySelector('#password').value = '{password}';
                    document.querySelector('.btn-login').click();
                ",
                
                _ => ""
            };
        }
        
        private string GetBetScript(BetOrder order)
        {
            return _config.Platform switch
            {
                "YunDing28" => $@"
                    document.querySelector('[data-play=""{order.PlayType}""]').click();
                    document.querySelector('input.bet-amount').value = {order.Amount};
                    document.querySelector('[data-option=""{order.BetContent}""]').click();
                    document.querySelector('button.confirm-bet').click();
                ",
                
                "BingGo28" => $@"
                    // 炳狗28 的投注脚本
                    // ...
                ",
                
                _ => ""
            };
        }
        
        #endregion
        
        private async Task WaitForLoad()
        {
            await Task.Delay(500);
        }
    }
}
```

---

## 🎨 UI 集成（也很简单）

### 快速设置面板

```csharp
// VxMain.cs

private AutoBetService _autoBet;

private void InitializeAutoBet()
{
    _autoBet = new AutoBetService(_db, _logService);
}

// 启动按钮
private async void BtnStartAutoBet_Click(object sender, EventArgs e)
{
    var defaultConfig = _autoBet.GetConfigs().FirstOrDefault(c => c.IsDefault);
    if (defaultConfig != null)
    {
        await _autoBet.StartBrowser(defaultConfig.Id);
        ShowSuccessTip("浏览器已启动");
    }
}

// 投注按钮
private async void BtnPlaceBet_Click(object sender, EventArgs e)
{
    var defaultConfig = _autoBet.GetConfigs().FirstOrDefault(c => c.IsDefault);
    if (defaultConfig != null)
    {
        var order = new BetOrder
        {
            PlayType = "大小",
            BetContent = "大",
            Amount = 100
        };
        
        var result = await _autoBet.PlaceBet(defaultConfig.Id, order);
        ShowSuccessTip(result.Success ? "投注成功" : "投注失败");
    }
}

// 配置管理器按钮
private void BtnConfigManager_Click(object sender, EventArgs e)
{
    var form = new BetConfigManagerForm(_autoBet, _logService);
    form.ShowDialog();
}
```

---

### 配置管理器窗口（简化版）

```csharp
public class BetConfigManagerForm : UIForm
{
    private readonly AutoBetService _autoBet;
    private UIDataGridView _dgvConfigs;
    
    public BetConfigManagerForm(AutoBetService autoBet, ILogService log)
    {
        _autoBet = autoBet;
        InitializeComponent();
        LoadConfigs();
    }
    
    private void LoadConfigs()
    {
        var configs = _autoBet.GetConfigs();
        _dgvConfigs.DataSource = configs;
    }
    
    // 新增配置
    private void BtnAdd_Click(object sender, EventArgs e)
    {
        var config = new BetConfig
        {
            ConfigName = "新配置",
            Platform = "YunDing28",
            PlatformUrl = "https://www.yunding28.com"
        };
        _autoBet.SaveConfig(config);
        LoadConfigs();
    }
    
    // 启动浏览器
    private async void BtnStart_Click(object sender, EventArgs e)
    {
        var selectedConfig = GetSelectedConfig();
        if (selectedConfig != null)
        {
            await _autoBet.StartBrowser(selectedConfig.Id);
            ShowSuccessTip("已启动");
        }
    }
}
```

---

## 🔄 配置变更处理（极简方案）

### 方案：配置变更时，提示用户重启浏览器

```csharp
// 用户修改配置
private void BtnSave_Click(object sender, EventArgs e)
{
    var config = GetSelectedConfig();
    
    // 保存配置
    _autoBet.SaveConfig(config);
    
    // 🔥 如果浏览器正在运行，提示重启
    if (IsBrowserRunning(config.Id))
    {
        var result = ShowAskDialog("配置已修改，需要重启浏览器才能生效。是否立即重启？");
        if (result)
        {
            _autoBet.StopBrowser(config.Id);
            await Task.Delay(500);
            await _autoBet.StartBrowser(config.Id);
        }
    }
    
    ShowSuccessTip("配置已保存");
}
```

**就这么简单！不需要复杂的"智能判断"！**

---

## 📊 代码量对比

| 方案 | 核心类数量 | 总代码行数 | 复杂度 |
|------|-----------|-----------|--------|
| 之前的设计 | 3-4 个类 | ~1000+ 行 | 高 |
| **极简方案** | **2 个类** | **~500 行** | **低** |

### 极简方案的文件结构

```
Services/AutoBet/
├── AutoBetService.cs      (~300 行) ✅ 唯一核心类
└── Browser.cs             (~200 行) ✅ 浏览器封装

Models/AutoBet/
├── BetConfig.cs           (~50 行)  ✅ 配置模型
├── BetOrder.cs            (~30 行)  ✅ 订单模型
├── BetOrderRecord.cs      (~40 行)  ✅ 订单记录
└── BetResult.cs           (~20 行)  ✅ 投注结果

Views/AutoBet/
└── BetConfigManagerForm.cs (~200 行) ✅ 配置窗口
```

**总共 ~850 行代码，搞定所有功能！**

---

## ✅ 为什么这个方案更好？

### 1. 极简设计
```csharp
// ❌ 过度设计
PlatformScriptFactory.Create(platform) → IPlatformScript → GetLoginScript()

// ✅ 极简设计
switch (platform) { case "YunDing28": return "..."; }
```

### 2. 直观易懂
- 只有 2 个核心类
- 没有复杂的抽象
- 代码即文档

### 3. 易于维护
- 新增平台：在 `GetLoginScript()` 和 `GetBetScript()` 中加一个 case
- 修改逻辑：直接找到对应方法修改
- 调试简单：没有复杂的调用链

### 4. 满足所有需求
- ✅ 多配置管理
- ✅ 浏览器启停
- ✅ 自动登录
- ✅ 投注下单
- ✅ 订单记录

---

## 🎯 扩展示例

### 新增平台（只需加 2 个 case）

```csharp
// 1. 在 GetLoginScript() 加一个 case
"KuaiLe8" => $@"
    document.querySelector('.username').value = '{username}';
    document.querySelector('.password').value = '{password}';
    document.querySelector('.login').click();
",

// 2. 在 GetBetScript() 加一个 case
"KuaiLe8" => $@"
    // 快乐8 的投注脚本
",
```

**就这么简单！不需要创建新类、新接口！**

---

## 🚫 不需要的东西

### ❌ 工厂模式
```csharp
// 不需要！
public static IPlatformScript Create(string platform) { ... }
```
**理由**: 只有几个平台，简单 switch 就够了

### ❌ 策略模式
```csharp
// 不需要！
public interface IPlatformScript { ... }
public class YunDing28Script : IPlatformScript { ... }
```
**理由**: 增加复杂度，收益很小

### ❌ 状态管理器
```csharp
// 不需要！
public class BrowserStateManager { ... }
```
**理由**: 简单的 string 状态就够了

### ❌ 配置变更监听器
```csharp
// 不需要！
public event EventHandler<ConfigChangedEventArgs> ConfigChanged;
```
**理由**: 直接提示用户重启更简单

---

## 🎉 总结

### 设计原则

1. **KISS**: Keep It Simple, Stupid
2. **YAGNI**: You Aren't Gonna Need It
3. **实用主义**: 能用简单方法就用简单方法

### 核心思想

```
简单 > 复杂
直接 > 抽象
实用 > 优雅
少写 > 多写
```

### 最终方案

- **2 个核心类**：`AutoBetService` + `Browser`
- **~500 行代码**：完成所有功能
- **无过度设计**：没有不必要的抽象
- **易于维护**：代码清晰、逻辑直观

---

## 📝 实施建议

### Week 1: 核心功能
1. 实现 `AutoBetService` 类（配置管理 + 浏览器控制）
2. 实现 `Browser` 类（CEF 封装）
3. 测试启动和登录

### Week 2: 投注功能
1. 实现 `GetBetScript()` 方法
2. 实现订单记录保存
3. 测试投注流程

### Week 3: UI 和完善
1. 实现配置管理器窗口
2. 集成到快速设置面板
3. 完善错误处理和日志

---

**这就是最实用、最简洁的方案！** ⭐

- ✅ 代码少（~500 行核心代码）
- ✅ 逻辑清晰（2 个类搞定）
- ✅ 易于维护（没有过度设计）
- ✅ 满足需求（所有功能都有）

**不要过度设计，实用才是王道！** 🚀

