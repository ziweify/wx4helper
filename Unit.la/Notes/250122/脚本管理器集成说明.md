# 脚本管理器已集成 - 界面说明

## ✅ 已完成集成

**脚本管理器**已成功集成到 `BrowserTaskControl` 的**脚本标签页**中！

## 🎨 新的界面布局

当您点击浏览器任务窗口中的 **"📝 脚本"** 标签时，您将看到：

```
┌───────────────────────────────────────────────────────────────────┐
│  ⚙️ 配置   📋 日志   📝 脚本  ← 点击这里                         │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌─────────────────────┬───────────────────────────────────────┐ │
│  │  脚本管理器 (左侧)   │   脚本编辑器 (右侧)                   │ │
│  │                     │                                        │ │
│  │  ● 📁 本地文件      │   ▶ 执行脚本 │ ✓ 验证脚本 │ 📖 函数帮助 │ │
│  │  ○ 🌐 远程URL      │   ┌──────────────────────────────────┐ │ │
│  │                     │   │                                  │ │ │
│  │ ─────────────────── │   │  -- Lua脚本编辑器                │ │ │
│  │ 本地目录:           │   │  log('Hello World')              │ │ │
│  │ [____________]      │   │                                  │ │ │
│  │ [浏览][创建模板][刷新]│   │  function main()                 │ │ │
│  │ 💡 选择lua文件夹    │   │      -- 您的代码                 │ │ │
│  │                     │   │  end                             │ │ │
│  │ ─────────────────── │   │                                  │ │ │
│  │ 脚本列表:           │   │  main()                          │ │ │
│  │ ┌─────────────────┐│   │                                  │ │ │
│  │ │ 🚀 main         ││   │  ← 语法高亮                      │ │ │
│  │ │ 📚 functions    ││   │  ← 断点调试                      │ │ │
│  │ │ 🧪 test         ││   │  ← 行号显示                      │ │ │
│  │ │ 📄 custom1      ││   │                                  │ │ │
│  │ └─────────────────┘│   └──────────────────────────────────┘ │ │
│  │                     │                                        │ │
│  │ [➕ 新建] [🗑 删除]  │                                        │ │
│  │ [📂 打开文件夹]     │                                        │ │
│  └─────────────────────┴───────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────┘
```

## 🎯 功能说明

### 左侧：脚本管理器 (300px 宽)

#### 1. 模式切换
- **● 📁 本地文件** (默认选中)
  - 从本地文件夹加载 `.lua` 脚本
  - 可以新建、编辑、删除脚本
  
- **○ 🌐 远程URL**
  - 从远程服务器加载脚本（JSON格式）
  - 适合未来网络传输场景

#### 2. 本地模式面板
```
本地目录: [E:\Scripts\Task1________]
[浏览...] [创建模板] [🔄 刷新]
💡 提示: 选择包含 .lua 脚本文件的文件夹
```

**操作说明：**
- **浏览**：选择包含 `.lua` 文件的文件夹
- **创建模板**：一键创建 `main.lua`、`functions.lua`、`README.md` 标准脚本模板
- **刷新**：重新读取文件夹中的所有脚本文件

#### 3. 远程模式面板（点击远程URL后显示）
```
远程URL: [https://api.example.com/scripts]
认证Token: [***********] (可选)
[测试连接] [🔄 加载脚本]
📋 JSON格式: {"脚本a": "内容", "脚本b": "内容"}
```

**操作说明：**
- **远程URL**：输入脚本API地址
- **认证Token**：如果需要认证，填写Token（密码字符显示）
- **测试连接**：测试远程API是否可访问
- **加载脚本**：从远程加载所有脚本到内存

#### 4. 脚本列表
```
脚本列表:
┌───────────────────┐
│ 🚀 main (主脚本)   │ ← 自动识别类型
│ 📚 functions (功能库) │
│ 🧪 test (测试脚本)  │
│ 📄 custom1 (自定义) │
└───────────────────┘
```

**操作说明：**
- **单击**：选中脚本
- **双击**：在右侧编辑器中打开脚本内容

#### 5. 操作按钮
```
[➕ 新建脚本] [🗑 删除] [📂 打开文件夹]
```

**操作说明：**
- **新建脚本**：弹出对话框，输入脚本名称和类型，创建新脚本文件
- **删除**：删除选中的脚本文件（仅本地模式，会提示确认）
- **打开文件夹**：用资源管理器打开脚本所在文件夹（仅本地模式）

### 右侧：脚本编辑器

#### 顶部工具栏
```
▶ 执行脚本 │ ✓ 验证脚本 │ 📖 函数帮助
```

**按钮说明：**
- **▶ 执行脚本**：执行当前编辑器中的脚本
- **✓ 验证脚本**：检查脚本语法是否正确
- **📖 函数帮助**：查看所有可用的Lua函数列表和示例

#### 编辑器功能
- ✅ **语法高亮**：Lua语法高亮显示
- ✅ **行号显示**：左侧显示行号
- ✅ **断点调试**：点击行号左侧设置断点
- ✅ **错误标记**：实时显示语法错误
- ✅ **代码折叠**：折叠函数和代码块
- ✅ **自动完成**：代码提示功能

## 📊 使用流程

### 场景1：首次使用（本地模式）

1. **点击"📝 脚本"标签页**
2. **选择脚本目录**：
   - 点击左侧的 **[浏览...]** 按钮
   - 选择一个空文件夹或创建新文件夹
3. **创建模板脚本**：
   - 点击 **[创建模板]** 按钮
   - 自动创建 `main.lua` 和 `functions.lua`
4. **编辑脚本**：
   - 在脚本列表中**双击** `main.lua`
   - 右侧编辑器显示脚本内容
   - 修改脚本并保存（Ctrl+S）
5. **测试脚本**：
   - 点击 **▶ 执行脚本** 运行
   - 或点击 **✓ 验证脚本** 检查语法

### 场景2：切换到远程模式

1. **点击"📝 脚本"标签页**
2. **切换模式**：
   - 点击 **○ 🌐 远程URL** 单选按钮
   - 本地配置面板隐藏，远程配置面板显示
3. **配置远程URL**：
   - 输入：`https://api.example.com/scripts/task1`
   - 如需认证，填写Token
4. **测试连接**：
   - 点击 **[测试连接]** 按钮
   - 查看连接状态（✅ 成功 / ❌ 失败）
5. **加载脚本**：
   - 点击 **[🔄 加载脚本]** 按钮
   - 从远程API加载所有脚本到内存
   - 脚本列表自动更新
6. **编辑和执行**：
   - 双击选中的脚本
   - 在编辑器中修改（仅内存模式）
   - 执行或验证脚本

### 场景3：管理多个脚本

1. **新建自定义脚本**：
   - 点击 **[➕ 新建脚本]** 按钮
   - 填写：
     - 脚本文件名：`login.lua`
     - 显示名称：`登录脚本`
     - 脚本类型：`自定义`
   - 点击确定
2. **在列表中选择脚本**：
   - 脚本列表显示新创建的脚本
   - 双击打开编辑
3. **删除不需要的脚本**：
   - 单击选中要删除的脚本
   - 点击 **[🗑 删除]** 按钮
   - 确认删除

## 🔧 技术细节

### 数据流

```
┌─────────────────┐
│ ScriptManager   │  ← 用户操作
│ Control         │
└────────┬────────┘
         │
         ├─ 选择脚本 (双击)
         │  └→ ScriptSelected 事件
         │     └→ 加载到编辑器
         │
         ├─ 切换模式 (单选按钮)
         │  └→ ConfigChanged 事件
         │     └→ 更新 BrowserTaskConfig
         │
         └─ 脚本更新
            └→ ScriptsUpdated 事件
               └→ 记录日志
```

### 配置持久化

```csharp
// 配置中新增的字段
public class BrowserTaskConfig
{
    // 原有字段...
    public string Name { get; set; }
    public string Url { get; set; }
    public string Script { get; set; }
    
    // ✅ 新增字段
    public string? ScriptDirectory { get; set; }        // 脚本目录路径
    public ScriptSourceMode ScriptSourceMode { get; set; }  // Local / Remote
}
```

## 🎉 优势总结

### 1. 用户体验
✅ **一目了然**：单选按钮清晰显示当前模式  
✅ **即时反馈**：加载状态实时显示（⏳/✅/❌）  
✅ **图标化**：脚本类型图标，快速识别  
✅ **分离布局**：管理和编辑分开，互不干扰  

### 2. 功能完整
✅ **双模式**：本地文件 + 远程URL  
✅ **完整管理**：新建、删除、刷新、打开文件夹  
✅ **模板支持**：一键创建标准脚本结构  
✅ **实时编辑**：选择脚本后立即在编辑器显示  

### 3. 未来扩展
✅ **内存优先**：远程脚本在内存执行（适合网络传输）  
✅ **元数据支持**：记录脚本来源、加载时间等  
✅ **接口预留**：可扩展网络保存、版本控制等功能  

## 📝 代码示例

### 从脚本管理器加载脚本到编辑器

```csharp
// 订阅脚本选中事件
scriptManager.ScriptSelected += (s, scriptInfo) =>
{
    if (scriptInfo != null && _scriptEditor != null)
    {
        // 加载脚本到编辑器
        _scriptEditor.ScriptText = scriptInfo.Content;
        
        // 记录日志
        LogMessage($"📄 已加载脚本: {scriptInfo.DisplayName}");
    }
};
```

### 保存脚本配置

```csharp
// 订阅配置变更事件
scriptManager.ConfigChanged += (s, sourceConfig) =>
{
    // 更新任务配置
    _config.ScriptDirectory = sourceConfig.LocalDirectory;
    _config.ScriptSourceMode = sourceConfig.Mode;
    
    // 记录日志
    LogMessage($"⚙️ 脚本源模式已切换为: {sourceConfig.Mode}");
};
```

---

**完成时间**: 2026-01-22  
**状态**: ✅ 已集成并通过编译  
**位置**: `Unit.la/Controls/BrowserTaskControl.cs` 的脚本标签页
